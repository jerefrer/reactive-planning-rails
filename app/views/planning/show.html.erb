<%= react_component('Scheduler', {
                      days: @days,
                      tasks: @tasks,
                      people: @people,
                      duties: @duties,
                      days_url: days_path,
                      duties_url: duties_path,
                      delete_duty_url: delete_duties_path
                    },
                    { prerender: false }) %>
